#include <stdio.h>

/**
 * _is_prime_number - returns 1 if the integer is prime
 * Description: returns 1 for a prime numer
 * @n: accepts an int
 * Return: returns an integer
 */

int _is_prime_number(int n)
{
	int num;

	if (num == i)
	{
		return (0);
	}
	else if (num % i == 0)
	{
		return (1);
	}
	else
	{
		return _is_prime_number(i + 1);
	}
}
