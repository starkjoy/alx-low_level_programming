#include "main.h"

/**
 * main - main prints the absolute of an integer
 *
 * Return: returns 0
 */

int _abs(int i)
{
	abs(i);
	printf("%i \n", i);

	return (i);
}


int main(void)
{
	_abs(12.7);

	return (0);
}
